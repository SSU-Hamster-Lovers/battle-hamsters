{
  "name": "@battle-hamsters/game",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --host ${GAME_HOST:-0.0.0.0} --port ${GAME_PORT:-5173}",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types src/pickup-vfx.test.ts"
  },
  "dependencies": {
    "@battle-hamsters/shared": "workspace:*",
    "@battle-hamsters/vfx-runtime": "workspace:*",
    "phaser": "^3.80.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
